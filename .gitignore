node_modules/
.turbo/
dist/
.next/
.expo/
# Continuous Native Generation: app.json is the source of truth and `expo prebuild`
# regenerates these wholesale. Committing them would let a stale manifest outlive a change
# to app.json -- and android/ is where `allowBackup="false"` has to land.
apps/mobile/android/
apps/mobile/ios/
.env
.env.local
.env.*
!.env.example
*.tsbuildinfo
next-env.d.ts
expo-env.d.ts
supabase/.temp/
.superpowers/
.claude/worktrees/
